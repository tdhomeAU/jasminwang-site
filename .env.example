# Local preview defaults to a simulated Kit success. Never enable this in production.
KIT_MOCK_MODE=true

# Configure these as server-side secrets/variables in the deployment environment.
KIT_API_KEY=
KIT_FORM_ID=
